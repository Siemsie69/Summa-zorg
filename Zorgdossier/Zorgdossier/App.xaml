<Application x:Class="Zorgdossier.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Zorgdossier"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:views="clr-namespace:Zorgdossier.Views"
             xmlns:viewModels="clr-namespace:Zorgdossier.ViewModels">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme BaseTheme="Light"/>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Aangepaste Kleuren -->
            <SolidColorBrush x:Key="PrimaryBrush" Color="#240d68" />
            <SolidColorBrush x:Key="SecondaryBrush" Color="White" />

            <!-- Material Design Aangepaste Kleurvervanging -->
            <SolidColorBrush x:Key="MaterialDesignPrimaryColor" Color="#240d68" />
            <SolidColorBrush x:Key="MaterialDesignSecondaryColor" Color="White" />

            <!-- DataTemplates for ViewModels -->
            <!-- <DataTemplate DataType="{x:Type viewModels:RegistrationViewModel}">
                <views:RegistrationView />
            </DataTemplate> -->

            <DataTemplate DataType="{x:Type viewModels:MainViewModel}">
                <views:MainView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type viewModels:HomeViewModel}">
                <views:HomeView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type viewModels:DossiersViewModel}">
                <views:DossiersView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type viewModels:ExplanationViewModel}">
                <views:ExplanationView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type viewModels:CreditsViewModel}">
                <views:CreditsView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type viewModels:SettingsViewModel}">
                <views:SettingsView />
            </DataTemplate>
        </ResourceDictionary>
    </Application.Resources>
</Application>